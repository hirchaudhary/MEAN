<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>All Quotes</title>
    </head>
    <body style="text-align:center">
        <h1>Here are the awesome quotes!</h1>
        <% for(let quote in quotes) {%>
            <strong><%=quotes[quote].quote%></strong>
            <p>- <%=quotes[quote].name%> at <%= moment(quotes[quote].createdAt).format( 'hh:mma MMMM D YYYY') %></p>
            <hr>
        <% } %>
    </body>
</html>
